version: "2"

options:
  verbose: true
syncs:
  codebase-docker-sync: # tip: add -sync and you keep consistent names as a convention
    src: "./www"
    # sync_strategy: 'native_osx' # not needed, this is the default now
